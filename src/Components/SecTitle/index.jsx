import React from "react";

const SecTitle = ({ title }) => {
  return (
    <p className="text-primaryText font-bold text-3xl text-center py-3">
      {title}
    </p>
  );
};

export default SecTitle;
